{% extends 'base.html.twig' %}

{% block title %}Fiche de {{ editor.name }}{% endblock %}

{% block body %}

<div class="example-wrapper py-5">
    <h1>Auteur : </h1>
    {% if editor is not null %}
        <div class="card mb-1 m-auto">
            <div class="card-body">
                <div class="card-title d-flex justify-content-between">
                    <h4 class="mb-1">{{ editor.name }}</h4>
                    <small class="text-muted">Identifiant : {{ editor.id }}</small>
                </div>
                
            </div>
        </div>
    {% else %}
        <div>Auteur non trouvé</div>
    {% endif %}

    <a href="{{ path('app_admin_editor_index') }}" class="btn btn-primary mt-2
    ">Retour</a>
</div>
{% endblock %}